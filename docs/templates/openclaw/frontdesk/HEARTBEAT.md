# HEARTBEAT.md

# Keep this file comments-only to disable periodic heartbeat actions.

# Example tasks (enable only if needed):
# - every 15m: check Core discovery endpoint health.
# - every 15m: verify invoke round-trip latency under threshold.
# - every 1h: verify discovery payload still includes critical tools.
